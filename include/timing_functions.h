#pragma once

#include "kernel_functions.h"
#include "linked_list.h"

// ? is this supposed to be the same functions ?
// exception kern_wait(uint nTicks); -> declaration in kernel_functions.h
exception wait(uint nTicks);


